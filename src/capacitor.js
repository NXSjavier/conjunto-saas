export async function initCapacitorNotifications() {
  // Push notifications deshabilitado - requiere Firebase/Google Services
  // Para habilitar: crear proyecto Firebase, descargar google-services.json
  // y colocarlo en android/app/google-services.json
}
